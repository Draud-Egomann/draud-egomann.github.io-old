@using Web.Components.Components.Misc

<div class="own-bg-primary text-white flex items-center justify-center lg:min-h-screen @(Class)">
  <div class="container lg:min-h-screen relative flex justify-center items-center m-8">
    <div class="flex flex-col items-center gap-8">
      <img src=@ImageUrl alt="Image of myself" class="size-full sm:size-96" />

      <div class="border-b-8 own-border-accent w-full lg:w-[40rem]"></div>

      <Heading Size="HeadingSize.Is2" Class="italic">
        @Text
      </Heading>
    </div>

    <AngleVector Class="hidden lg:block absolute top-0 left-0 rotate-180" Width="200" Height="200" Stroke="#FD2C4D" StrokeWidth="16" />
    <AngleVector Class="hidden lg:block absolute bottom-0 right-0" Width="200" Height="200" Stroke="#FD2C4D" StrokeWidth="16" />
  </div>
</div>

@code {
  [Parameter]
  public string Class { get; set; } = string.Empty;

  [Parameter]
  [EditorRequired]
  public string ImageUrl { get; set; } = default!;

  [Parameter]
  [EditorRequired]
  public string Text { get; set; } = default!;
}
